/**
 * AI_NOTE:
 * Role: Home route entry.
 * Keep this file minimal; route-level composition should stay delegated to HeroScene.
 */

import { HeroScene } from "@/components/hero-scene";

export default function Home() {
  return <HeroScene />;
}
